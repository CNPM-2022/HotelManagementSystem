import MainContent from './MainContent';
import TopContent from './TopContent';

function Header() {
    return (
        <header>
            <TopContent />
            <MainContent />
        </header>
    );
}

export default Header;
